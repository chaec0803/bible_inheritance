# 회귀 테스트 기준선 결과

실행일: 2026-09-03

## 자동 테스트

- 전체 36개 테스트 파일, 142개 테스트 통과.
- 말씀 선물 기능 15건 통과: 녹음 순서·BGM 정책, 친구·소유권 검증, 선물 전용 R2 복사, 수신자 목록·재생·ZIP 다운로드·삭제 권한, 보내기·선물함 UI와 내비게이션.
- 친구 기능 15건 통과: 검색어·닉네임 정책, 사용자 쌍 정규화, 인증·요청·수락 권한, 이메일 마스킹, 친구 화면과 내비게이션.

- 일정 정책 6건 통과: 최초 1일차, 정상 전진, 결석 고정, 여러 날 결석 시 1단계 제한, 당일 재전진 방지, 마지막 날 상한.
- 말씀카드 보상 정책 5건 통과: 최초 지급, 일차별 중복 방지, 미보유 카드 우선, 전체 수집 후 재순환, 간직하기 상태 전환.
- 카드 이미지 성능 예산 3건 통과: 모든 스프라이트 WebP 및 파일당 500KB 이하.
- 홈 탐색 정책 1건 통과: 매일 말씀 읽기 선택 화면의 뒤로가기는 내 말씀 여정이 아닌 홈으로 이동.
- 성경 데이터 2건 통과: 66권 고유 코드, 모든 책의 JSON·장 수·절 수·빈 본문 전수 검증.
- API 권한 스모크 통과: 소유자 키 없는 보관함 목록과 오디오 요청 모두 HTTP 401.
- TypeScript, oxlint, 프로덕션 빌드, diff whitespace 검사를 배포 전 필수 게이트로 사용한다.

`pnpm lint:all`에는 현재 제품에서 사용하지 않는 생성형 `components/ui`와 `hooks/use-mobile.ts`의 기존 접근성·React Compiler 경고 18건이 남아 있다. 제품 회귀 게이트인 `pnpm lint`는 실제 앱·도메인·DB 코드에 적용하며, 전체 템플릿 부채는 별도 명령으로 계속 추적한다.

## 배포 브라우저 스모크

- 홈 우선 진입/새로고침 후 홈 복귀: 통과.
- 홈 라이트·다크 모드 전환: 통과.
- 자유 녹음 성경 선택 진입: 통과.
- 구약 39권/신약 27권 표시: 통과.
- 마태복음 28장 및 1장 25절 로딩: 통과.
- 선택 장 녹음 CTA와 마태복음 1장 녹음 화면 진입: 통과.
- 25절 목록, 다음 절 이동: 통과.
- 리버브 없이 원음 녹음 및 BGM 없음 선택: 통과.
- 빈 여정 보관함과 첫 녹음 진입 CTA: 통과.
- 1주 여정 템플릿 4종 표시: 통과.

## 하드웨어·외부 서비스 의존 수동 테스트

- 실제 마이크 권한 승인, 장시간 녹음, 모바일 Safari/Chrome 음질.
- YouTube 플레이어의 실제 BGM 재생·일시정지·정지 및 네트워크 오류 복구.
- 실제 녹음 파일을 이용한 이어듣기 절 전환, 절별 삭제, 장 전체 삭제.

위 항목은 사용자 음성을 생성·삭제하거나 브라우저 권한 승인이 필요하므로 자동 스모크에서 임의 수행하지 않는다. 테스트 전용 녹음 fixture와 격리된 테스트 소유자 계정을 추가하면 다음 단계에서 완전 자동화할 수 있다.

## 2026-09-06 녹음 코어 통합 후 로컬 UI 회귀

테스트 계정: `winniethepoox3@gmail.com` · 로컬 개발 서버

- 일반 녹음: 오바댜 1장 새 자유 녹음에서 1절 시작, 2절 경계 이동, `여기까지 녹음`, 저장 로딩, 두 절 저장 완료를 확인했다.
- 일반 녹음 재생: 완료 모달을 닫은 뒤 2절 저장본의 실제 재생 시간 진행과 일시정지 상태를 확인했다.
- 선물 절별 교체: 기존 시편 23편 초안의 6절을 삭제 후 다시 녹음해 `6/6` 복구와 즉시 재생을 확인했다.
- 선물 연속 녹음: 5·6절을 비운 뒤 5절 저장 성공 후에만 6절로 이동하고 녹음이 다시 시작되는 것을 확인했다.
- 선물 종료: `마지막 절 완료`와 `현재 절까지 저장` 모두 저장 중 로딩, `6/6` 복구, 절별 재생 및 쪽지 단계 CTA를 확인했다.
- 음성 편지: 녹음 시작, 종료, 미리듣기 생성, 확정, 전송 버튼 활성화를 확인했다. 실제 전송은 수행하지 않았다.
- BGM: 재생, 일시정지, 정지 활성 상태와 음량 슬라이더 2% 반영을 확인했다.
- 테스트 중 브라우저 콘솔 error/warn은 없었다.

이 테스트는 로컬 DB와 로컬 음성 저장소만 변경했으며 배포는 수행하지 않았다.
